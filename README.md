To run the Python file, you first need to install the Streamlit framework. Then, open the terminal or command prompt in the folder where your Python file is located and enter the following command:
"streamlit run Student_Dashboard.py"
This command will start the Streamlit app and open it in your default web browser.
